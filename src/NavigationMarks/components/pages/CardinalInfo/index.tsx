import {h} from 'preact'
import * as Type from '../../../types'
import './index.scss'

type Props = {

}

export const CardinalInfo: Type.F<Props> = ({}) => {
  return (
    <div class='cardinal'>
      CardinalInfo
    </div>
  )
}